spring:
  application:
    name: MSTxFleet-Trip
  cloud:
    discovery:
      enabled: true

server:
  port: 0

service:
  postgis:
    name: MSTXFLEET-DBS-POSTGIS
    url: localhost:8888/MSTXFLEET-DBS-POSTGIS